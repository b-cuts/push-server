{
   "_id": "_design/api-request",
   "language": "javascript",
   "views": {
       "allApiRequests": {
           "map": "\nfunction(doc) {\n  if (doc.entityType == 'api-request') {\n    emit(doc._id, null);\n  }\n}"
       },
       "byClientAndType": {
           "map": "\nfunction(doc) {\n  if (doc.entityType == 'api-request' && doc.entity.pushType) {\n    emit([doc.entity.apiClientId, doc.entity.pushType], null);\n  }\n}"
       },
       "byClientAndSession": {
           "map": "\nfunction(doc) {\n  if (doc.entityType == 'api-request' && doc.entity.pushType == 'userEvent' && doc.entity.push.sessionId) {\n    emit([doc.entity.apiClientId, doc.entity.push.sessionId], null);\n  }\n}"
       },
       "byClientAndDevice": {
           "map": "\nfunction(doc) {\n  if (doc.entityType == 'api-request' && doc.entity.pushType == 'userEvent' && doc.entity.push.deviceId) {\n    emit([doc.entity.apiClientId, doc.entity.push.deviceId], null);\n  }\n}"
       },
       "byClient": {
           "map": "\nfunction(doc) {\n  if (doc.entityType == 'api-request' && doc.entity.pushType) {\n    emit(doc.entity.apiClientId, null);\n  }\n}"
       }
   }
}